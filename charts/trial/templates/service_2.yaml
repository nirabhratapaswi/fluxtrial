apiVersion: v1
kind: Service
metadata:
  name: {{ include "fluxtrial.fullname" . }}-2
  labels:
    {{- include "fluxtrial.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: 8002
      targetPort: 8002
      protocol: TCP
      name: tcptrial2
  selector:
    {{- include "fluxtrial.selectorLabels" . | nindent 4 }}
